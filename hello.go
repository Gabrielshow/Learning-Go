// Go source-code is stored in .go files, these filenames
// consist of lowecase-letters, like scanner-go
// if the name consist of multiple parts, there are separated by underscores  , like scanner_test.go
// Filenames may not contain spaces or any special characters.

// Go is case sensitive
// The _ itself is a special identifier, called the blank identifier. it can be sued in declarations or variable assignments like any other identifier (and any type can be assigned to it),
// but its value is discarded, so it cannot be used anymore in the code that follows.

package main

import "fmt"

func main() {
	fmt.Println("hello, world")
}
